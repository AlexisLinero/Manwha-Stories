@{
    ViewData["Title"] = "Manwha Stories - Inicio";
}

<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <style>
        .hero-section {
            background-image: url('https://azure-merchants.zammit.shop/active-storage/u9cuxn9rvquyt2b9zvmug48wxt6c');
            background-size: cover;
            background-position: center;
            color: #ffffff;
            text-align: center;
            padding: 100px 20px;
            position: relative;
            z-index: 1;
        }

            .hero-section::before {
                content: '';
                position: absolute;
                top: 0;
                left: 0;
                right: 0;
                bottom: 0;
                background-color: rgba(0, 0, 0, 0.5);
                z-index: 0;
            }

        h1, p {
            position: relative;
            z-index: 1;
        }

        .btn-catalogo, .btn-inventario {
            color: white;
            padding: 15px 30px;
            font-size: 20px;
            border: none;
            border-radius: 5px;
            transition: background-color 0.3s, transform 0.3s;
            z-index: 1;
            position: relative;
        }

        .btn-catalogo {
            background-color: #ff5722;
        }

        .btn-inventario {
            background-color: #2196f3; /* Color para el botón de inventario */
        }

        .btn-catalogo:hover {
            background-color: #e64a19;
            transform: scale(1.05);
        }

        .btn-inventario:hover {
            background-color: #1976d2;
            transform: scale(1.05);
        }
    </style>
</head>
<body>
    <div class="hero-section">
        <h1>Bienvenido a Mawha Store</h1>
        <p>Aquí podrás encontrar los mejores productos de anime.</p>
        <a href="/Catalogo" class="btn btn-catalogo">Ver Catálogo</a>
    </div>
</body>
</html>
